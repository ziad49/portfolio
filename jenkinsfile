pipeline {
    agent any
    tools { nodejs 'node' }
    stages {
        stage('Récupération') {
            steps { checkout scm }
        }
        stage('Installation') {
            steps { bat 'npm install' }
        }
        stage('Tests Unitaires') {
            steps { bat 'npm test -- --watchAll=false' }
        }
    }
}